{
  "name": "project__yanki-shop_js",
  "version": "1.0.0",
  "description": "---",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-with-build": "npm-run-all --parallel build-client start-server",
    "build-client": "cd client && npm run build",
    "start-server": "cd server && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
