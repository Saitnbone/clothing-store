// Переменные для страницы каталога

// const catalog = document.querySelector(".")

// Функции

function counter() {
  // напишите свой код здесь
  let count = 0;
  return (count += 1);
}
let el = counter();

console.log(el); // 1
console.log(el); // 2
counter(); // 3
counter(); // 4
counter(); // 5
